
  # Document Intelligence Prototype

  This is a code bundle for Document Intelligence Prototype. The original project is available at https://www.figma.com/design/a0hRo1huOZeCx6Q0jTd77N/Document-Intelligence-Prototype.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  